# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration (PostgreSQL URL)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/primetrade?schema=public"

# Authentication Secrets (Generate strong random strings for production)
JWT_SECRET="super-secret-jwt-access-key-replace-in-production"
JWT_REFRESH_SECRET="super-secret-jwt-refresh-key-replace-in-production"

# CORS Configuration
CORS_ORIGIN="http://localhost:3000"
